on: 
  push:
    branches:
      - main
    
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2.3.2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        server: ftp.mobislimited.com
        username: dev@outsmart-ideas.ebsgov.org
        password: ${{ FTP_PASSWORD }}
        server-dir: /home/mobijvrl/outsmart-ideas.ebsgov.org/dev
